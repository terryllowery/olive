<br />
<div class="booyah-box">
  <h1 class="text-center"><%= @course.title %></h1>
  <br />
  <%= link_to 'Preview As Student...', course_path(@course), class: 'btn btn-warning float-right' %>
  <br class="clr" />
  <h2 class="text-center">Sections</h2>
  <%= link_to 'Add new section', new_instructor_course_section_path(@course), class: 'btn btn-primary btn-large'%>

  <ul>
    <% @course.sections.each do |section| %>
    <li>
        <%= section.title %>
        <%= link_to 'New Lesson', new_instructor_section_lesson_path(section), class: 'float-right btn btn-primary btn-sm' %>
        <ul>
          <% section.lessons.each do |lesson| %>
            <li>
              <b><%= lesson.title %></b>
              -
              <%= lesson.subtitle %>
            </li>
          <% end %>
        </ul>
      <br class="clr" />
    </li>
    <% end %>
  </ul>


</div>